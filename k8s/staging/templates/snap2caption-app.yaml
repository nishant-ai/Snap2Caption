# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: snap2caption-app
  namespace: snap2caption-staging

spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: snap2caption-app
  template:
    metadata:
      labels:
        app: snap2caption-app
    spec:
      containers:
        - name: snap2caption-app
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: snap2caption-app
  namespace: snap2caption-staging
spec:
  selector:
    app: snap2caption-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  externalIPs:
    - {{ .Values.service.externalIP }}

